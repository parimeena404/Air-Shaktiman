'use client';

import React from 'react';
import { GdgFloatingBubbles } from './GdgFloatingBubbles';

/**
 * Animated Google / GDG floating dots and bubble theme background.
 * Replaces old squid cyber background with the living Google colors bubble wallpaper.
 */
export const SquidCyberBackground: React.FC = () => {
  return <GdgFloatingBubbles />;
};
